# LESSONS.md — 踩坑记录与已验证的教训

> 每条按「现象 → 走过的弯路 → 真根因 → 解法 → 防止再犯」记录。
> 时间线详见 [PROJECT.md](PROJECT.md) §6/§7；决策理由见 [DECISIONS.md](DECISIONS.md)。

## L1 误判方向：把"风控/内容审查"当成大请求失败的原因（2026-08-25）

- **现象**：OpenCode 小问题正常，一跑真实编码任务就报错。
- **弯路**：先后怀疑请求体积触发风控、敏感内容被审查、被识别为第三方调用。
- **排除过程**：做了 7 组对照实验（23 万字符大请求、敏感内容、多工具、多轮 agent
  历史等）——全部成功，假设全部被否。
- **教训**：「大请求才失败」是相关性不是因果性（真因是大任务几乎必然触发工具调用，
  见 L5/L6）。先设计对照实验分离变量，再下结论；不要急着给系统"拟人化"动机。

## L2 解析器 bug：CDATA 数组被当叶子文本（2026-08-25）

- **现象**：OpenCode 报 `SchemaError(Expected array, got "<item>{...}...")`。
- **真根因**：上游（moe_5）调 `question` 工具时把数组参数整体包进 CDATA 当文本返回，
  解析器照单全收 → `questions` 变成字符串 → OpenCode zod 校验失败。
- **解法**：`tool_parser.py` 增加恢复路径，把 CDATA 里的 `<item>...</item>` 序列解析回数组。

## L3 解析器 bug：幻觉闭合标签静默吞掉整个块（2026-08-25）

- **现象**：客户端收到空响应（`tool_calls=0`、空 delta），日志里没有明显报错。
- **真根因**：模型偶尔吐出多余的 `</arg_value>`（不存在的标签），XML 解析失败后被
  静默丢弃，整块工具调用消失。
- **解法**：`_drop_unmatched_close_tags()` —— 仅当 `ET.fromstring` 首次失败时删掉
  无主闭合标签重试一次；正常块零开销。
- **教训**：解析器失败路径不能静默吞掉，至少要留日志或回退信号，否则"空响应"类
  问题排查会非常痛苦。

## L4 补丁自己藏着死代码：skeleton 校验永远不通过（2026-08-25，最有价值的一条）

- **现象**：L2 的第一个补丁"看起来"写对了，但实际从不生效。
- **真根因**：skeleton 检查用 `ITEM_ENTRY_PATTERN.sub("[]", text)`，结果产生 `[],[]`，
  永远通不过 `fullmatch(r"[\s,;]*")` → 恢复路径是死代码。
- **发现方式**：拿真实故障样本手跑验证才发现，单测没覆盖到这条路径。
- **教训（已验证）**：
  1. 任何"恢复/兜底路径"的修复，必须用**真实故障样本**证明它会触发，
     不能靠读代码推断"应该会走这里"。
  2. 真实故障样本要存下来当回归测试（`/tmp/glm_bad_sample.txt` 等），修完即写测试。
  3. `sub` 的替换值会留在文本里参与后续匹配——写 pattern 清理逻辑时先 `repr()`
     看一眼中间结果。

## L5 "任何 part status=error 就 502"过于粗暴：分不清是谁的错（2026-08-25 深夜）

- **现象**：§6 修复后大任务仍约 50% 概率报 502 `GLM part status error`。
- **真根因**：网页版自带服务端执行层 `all-tools`，看到结构化工具调用会抢着自己执行
  OpenCode 的外来工具（bash/todowrite/question），必然失败，回一个
  `role:"tool", model:"all-tools", status:"error"` 的 part。这是**网页自己执行失败**，
  不是**回答失败**，但旧代码把两者混为一谈，直接作废整条流。
- **解法**：`glm_client.py` 新增 `is_server_side_tool_error_part()`，识别这类 part 并
  忽略（只在整个 event 全是这类错误时才降级为 warning）；工具调用照常透传给客户端，
  由客户端本地执行。
- **教训**：错误要分类处理。对接"别人家"的系统时，先问"这个错误状态码是谁发的、
  表达的是什么语义"，再决定致命还是可容忍。

## L6 "时好时坏"= 上游双后端随机路由（2026-08-25 深夜）

- **现象**：同样的请求，有时成功有时 502，像网络不稳。
- **真根因**：上游在 `moe_47`（文本后端）和 `moe_5`（agent 后端）之间随机路由。
  两个后端返回工具调用的形态完全不同（纯文本 DSML vs 结构化 tool_calls part），
  旧代码只兼容前者 → 失败率 ≈ 路由骰子 × 是否触发工具调用。
- **解法**：两种形态都兼容 + L5 的容错，不再受路由影响。
- **教训**：间歇性故障先怀疑"随机路由/多后端 A-B"，grep 日志里的 model/后端字段
  统计分布，比抓包快得多。

## L7 字符串化参数 + 坏历史雪崩（2026-08-25 深夜）

- **现象**：`todowrite` 的 `todos`（声明为 array）被包成 JSON 字符串，OpenCode 校验
  报 SchemaError；且错误会话历史里 prompt 重复 5 遍、坏调用+错误文本每轮重放。
- **真根因**：模型/网页解析器对 array/object 参数多套一层引号；OpenCode 自动重试时
  把 SchemaError 文本当 tool_result 回灌 → 历史被污染 → 每轮重放，越滚越大。
- **解法**：`tool_parser.py` 新增 `coerce_tool_arguments()`，按 schema 把字符串化的
  array/object/number/boolean 解码成真结构；DSML prompt 增加"禁止多包一层引号"规则。
  已用真实事故数据（6 条 todos）验证可解码。
- **教训**：
  1. 对接严格校验的客户端（zod 等），代理必须按 schema 做参数类型校正，不能假设
     "模型会输出正确类型"。
  2. agent 会话历史会重放：**修完 bug 先开新会话验证**，旧会话的污染历史既浪费
     token 又会干扰复现判断。

## L8 测试数据手写转义静默出错（2026-08-25）

- **现象**：新回归测试失败，断言 `'\\"todos\\":[{' in output` 不成立。
- **真根因**：测试里手写的多层 JSON 转义字符串，一个引号位置差了一位
  （`}"` 和 `"]` 之间），整串不是合法 JSON，`json.loads` 静默失败走原样返回。
- **解法**：测试数据改用程序化构造——`inner = json.dumps([...]); arg = json.dumps({"todos": inner})`。
- **教训**：多层嵌套的 JSON 测试数据一律 `json.dumps` 程序化构造，禁止手打转义。

## L9 think 模式免费通道截断："想了一堆却一句话没说"被伪装成正常结束（2026-08-25）

- **现象**：OpenCode 用 glm-5.3-think 跑长任务，模型实际干了约 5 分钟活，最后一轮无正文、
  无工具调用，思考停在半句话，`finish_reason=stop`，客户端以为完成直接收工。
- **真根因**：上游网页免费通道对 think 模式有每轮生成配额，思考吃光配额后线路被静默掐断、
  伪装正常结束。日志是判断依据（`响应收尾` 行的 text_len vs reasoning_len + 思考末尾是否半句）。
- **解法**：① agent 任务用非 think 版 glm-5.3（用户决定）；② 代理自动检测"退化解"并同会话
  补发"请继续"，最多 2 次（DECISIONS.md D11）；③ 所有截断路径都有明确 warning 日志。
- **教训**：模型"没说话但回合结束"时不要默认模型偷懒，先查日志确认是"真没产出"还是
  "思考被掐断"；`finish_reason=stop` 不保证是自然结束。

## 通用排查方法（本项目验证有效）

1. `.env` 开 `DEBUG_DUMP_ALL=true` → `/tmp/glm2api.log` 有完整上游请求体/SSE 原文。
2. 三步法：**grep 日志原文定现场 → 用真实样本本地重放 → 修完写回归测试**。
3. 怀疑路由/形态问题时，统计日志里的 `"model":"moe_` 字段分布。
4. 对照实验优先于理论推断（L1）。
5. 服务改动必须 `./stop.sh && ./start.sh`（8100 端口；8000 是用户的 Personal OS，严禁动）。
