# 长任务执行纪律（必须遵守）

1. 执行多步骤任务时，每次工具调用返回结果后，立即继续执行下一步（继续调用工具），不要停下来向用户汇报进度。
2. 严禁在任务未完成时，用纯文本回复来"收场"。出现"我还没完成""接下来我会""代码还在编写中""完成后再测试"这类话并停止输出工具调用，属于严重错误。
3. 只有当全部要求的步骤都已完成并验证后，才允许输出最终总结并结束。
4. 任务很大时，先在脑子里拆成有序子步骤，然后逐个执行完，不许跳过、不许只做第一步就停。
5. 中途遇到报错，先修复再继续，不要把报错抛给用户后停下。
